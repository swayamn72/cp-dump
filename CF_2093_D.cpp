#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using vi = vector<ll>;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    // mt19937_64 rng(chrono::steady_clock::now().time_since_epoch().count());
    ll t; cin >> t;
    while(t--){
        ll n,q; cin >> n >> q;
        while(q--){
            char ch1,ch2; cin >> ch1 >> ch2;
            if(ch1=='-'){
                ll rangeleft = 1, rangeright = pow(2,2*n); //1-16
                ll left = 1, right = pow(2,n); //1-4
                while(){
                    ll mid = (left+right)/2;
                    if(x>mid && )
                }
                
            }else{
                ll x; cin >> x;
                
            }
        }        
    }
}