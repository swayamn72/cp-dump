#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using ull = unsigned long long;
using vi = vector<ll>;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ll a,b; cin >> a >> b;
    double bb = (b*2.0)/3.0;
    if(a>bb) cout << "Yes";
    else cout << "No";
}